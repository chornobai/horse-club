extends ../components/base/layout

block variables
  - const title = "Максимус" //- на страницах кроме главной используйте О нас — ProjectName
  - const wrapperMods = ""
  - const hasHeader = true
  - const headerMods = ""
  - const hasFooter = true
  - const footerMods = ""
  - const currentPage = "index" //- для остальных страниц пишем пустую строку или индекс активного headerLinks

block main
  main
    //- на странице запрещен "голый" код
    //- все блоки вставляются из папки pug/components/blocks

    +about()
    +benefits()
    +trend()
    +clients()
    +horses()
    +form()
    +review()
    +map()

block modals
  +modal("appointment")
  +modal-success('success')
  +modal-photo('photo')
  +modal-review('review')
